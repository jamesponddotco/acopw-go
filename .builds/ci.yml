---
image: 'archlinux'
packages:
  - 'go'
  - 'make'
sources:
  - https://git.sr.ht/~jamesponddotco/acopw-go
tasks:
  - lint: |
      cd acopw-go
      export PATH="${HOME}/go/bin:${PATH}"
      make lint
  - test: |
      cd acopw-go
      make test
